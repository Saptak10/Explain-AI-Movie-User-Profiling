fastapi==0.136.3
uvicorn[standard]==0.48.0
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2
python-jose[cryptography]==3.5.0
asyncpg==0.30.0
numpy==2.4.6
# deployment branch only: this service loads a pre-exported ONNX bundle
# (see export_onnx.py on main) via onnxruntime instead of torch.
# `import torch` alone costs ~200MB of resident memory before touching any
# data, which was the dominant reason the full-PyTorch service didn't fit
# a memory-constrained free host -- onnxruntime's import footprint is far
# lighter since it has no autograd/training machinery at all.
onnxruntime==1.27.0
